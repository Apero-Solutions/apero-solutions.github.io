.. _leds_buttons:

=========================
LEDs, Buttons and Outputs
=========================

ModBridge Pro has three status LEDs, a digital output signal, and two
control buttons.


Status LEDs
===========

.. list-table::
   :widths: 30 70
   :header-rows: 1

   * - LED Name
     - States and Meaning
   * - Config Mode
     - **Slow blink** — device starting up |br|
       **Steady ON** — configuration access point is active |br|
       **OFF** — device is in normal operating mode
   * - Internet LED
     - **Steady ON** — internet is reachable |br|
       **OFF** — no internet connectivity
   * - Modbus RX Activity
     - **Brief pulse** — a valid Modbus response was received |br|
       **No pulses** — no Modbus traffic (check wiring, Slave ID, or baud rate)

.. |br| raw:: html

   <br/>

LED Diagnostic Patterns
------------------------

.. list-table::
   :widths: 30 70
   :header-rows: 1

   * - Internet LED state
     - Situation
   * - ON
     - Normal — internet connected.
   * - OFF
     - Internet lost — check router, Wi-Fi password, or Ethernet cable.
   * - Blinking fast
     - Device error — try power-cycling.  If the issue persists, contact
       Apero Solutions.

.. list-table::
   :widths: 30 70
   :header-rows: 1

   * - Modbus RX Activity LED state
     - Situation
   * - Pulses regularly
     - Modbus communication is healthy.
   * - Never pulses
     - No Modbus responses — check RS-485 wiring (A/B polarity), Slave ID,
       and baud rate / parity settings.
   * - Pulses then stops
     - Some devices responding, others not — verify individual Slave IDs.


No-Internet Alert Output
========================

ModBridge Pro provides a **24 V DC digital output** that activates when the
device loses internet connectivity.  This output can be wired directly into
any external system that accepts a digital input — for example, a PLC input
module, an alarm panel, or an indicator light.

.. list-table::
   :widths: 30 70
   :header-rows: 1

   * - Output State
     - Meaning
   * - 24 V (HIGH)
     - Internet connection lost
   * - 0 V (LOW)
     - Internet connection OK

This allows the loss of connectivity to be monitored and acted upon
independently of the ModBridge Pro web interface — useful in installations
where automated alerts or failover actions are required.


Control Buttons
===============

.. list-table::
   :widths: 25 20 55
   :header-rows: 1

   * - Button
     - Action
     - Effect
   * - Internet Reset
     - Hold for **3 seconds**
     - Re-opens the configuration Wi-Fi access point.
       All existing settings are **preserved**.
       Use this to change credentials or any configuration without wiping
       the device.
   * - Factory Reset (IO0)
     - Hold for **3 seconds**
     - Erases all settings and reboots to factory defaults.
       **All configuration is lost.**

.. danger::

   Factory Reset **permanently deletes** all configuration, including
   Modbus device definitions, IoT credentials, and network settings.
   There is no undo.

.. warning::

   The Factory Reset button is located **inside the ModbridgePro B15 enclosure** and
   is intentionally not accessible from the outside.  To perform a factory
   reset, carefully open the enclosure door and press the internal button.
   This design prevents accidental resets in the field.

.. note::

   Both buttons are active during the first **10 seconds** after power-on.
